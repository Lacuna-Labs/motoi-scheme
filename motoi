#!/bin/bash
# motoi — press this to talk to Motoi.
#
# Usage:
#   ./motoi              → Motoi 0.75 (SFT baseline)
#   ./motoi --mkA        → Motoi Grand Reveal Mk A
#   ./motoi --base       → base Qwen, no adapter (control)
#   ./motoi --adapter P  → any adapter path
#
# Inside: 'q' or ctrl-C to exit. 'reset' clears history.

exec /Users/alfred/code/forge/.venv/bin/python \
  /Users/alfred/code/motoi-scheme/scripts/motoi-chat.py "$@"
